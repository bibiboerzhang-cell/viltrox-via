# Settings Domain

Owns account/profile settings, permissions, budget controls, and rules panels.

Current migrated slice: shared settings formatting and guard helpers used by the Settings page. Settings, staff activation, permission, sync guard, platform crawl, and budget APIs are exposed through `api.ts`.
