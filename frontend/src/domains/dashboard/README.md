# Dashboard Domain

Owns the management dashboard page shell, mission header, metric cards, trend panels, and selected intelligence summaries.

Current migrated slice: KPI/trend/region/platform/content read-model helpers used by the Mission Control page. API fan-out and page rendering still remain in the legacy page shell.
