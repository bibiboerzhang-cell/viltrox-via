# V-KPI Campaign OS Mockup Reference

`preview__3_.html` is the source of truth for the Campaign OS / 我的项目 UI.

Implementation rules:
- React should translate this HTML into production components.
- Do not redesign copy, rhythm, shadows, radii, or interaction patterns unless the mockup changes first.
- Keep backend behavior real: use existing APIs and current dashboard data; do not add fake backend tables for the UI pass.
- Build in blocks so each section can be visually compared against the HTML.
