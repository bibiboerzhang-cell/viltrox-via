# 前后端能力差清单(车道四 · 2026-06-12)

> 引用波3 四态审结论不重审;两代工人中断阵亡,主控代笔。

## A. 后端已有,前端未接(接通成本一句)
| 能力 | 后端位置 | 前端现状 | 接通成本 |
|---|---|---|---|
| 员工外联日报 digests | `vkpi_staff_outreach_digests`(mig 033)+ automation 真跑 | 无任何视图 | 一卡/一页,~半窗 |
| 评论采集运行统计 | `comments/collector.stats(days)` | 无端点无视图 | 端点+卡,~半窗 |
| 合同生成模板目录扩展位 | template_catalog 支持任意新模板 | 前端写死渲染但模板增删零改动即生效 | 0(已通) |
| 周脉冲账目 | weekly_pulse.py → pulse-log(6/15 首跑) | 无站内展示 | 读 md 显示,~0.25窗 |
| KOL 池 RealER 影子列 | mig 111 + 91 行已算 | 列表/详情未显示 | 详情加一行+对照徽,~0.25窗(候 0号对照期裁) |
| 审计日志查询 | audit 表齐全 | 仅散点消费 | 管理页,~1窗 |

## B. 前端超前,后端缺(当前:无)
发票回填/合同润色/签署关联/物流同步/漏斗卡——本周双端齐落,B 栏清零。
唯一注意:前端 `enqueueLogisticsSync` 等依赖 `.env` token,新环境部署需带配置。

## C. 双向缺(规划在档,代码无)
| 件 | 状态 |
|---|---|
| N1 沟通 CRM 时间线 | spec 已档(骨架版),候排期 |
| E5 账号全量同步 | spec 已档,候"施工"字 |
| E6 真 since 游标 | spec 已档(列 108 已铺),候随 E5 |
| F3 grounding 双轨发现 | 设计 v2+「测」实测齐(单次 ~$0.015-0.05),候"施工"字 |
| 设置页权限矩阵 | 规划中(账户授权+模块可见性),老系统设置页可参照 |
| Events 33 模块全量 | 决断单建议 B(mockup 预览已可用),A 需 ~25 窗 |
