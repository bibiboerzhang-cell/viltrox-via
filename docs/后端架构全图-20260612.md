# V-KPI 后端架构全图(六路并行 X 光 · 2026-06-12)

> 纯只读测绘,非查 bug。10 只 agent 并行(6 域 + 4 特查),95 万 tokens,342 次工具调用,每个声称带 file:行号并开文件核实。
> 指纹对账:**审计本身纯只读零写**;但测绘窗口内 `vkpi_kol_pool` 由 **1128 → 1134**(+6 行),SUM **32828.7260 守恒不变**、scored 仍 999。
> 漂移源**非审计**,是探针5 那条废数据 smart_search 级联(异步后台,job 944 血缘)持续发现新 TikTok/YouTube 创作者插库(id 5668-5673,source=manual/created_by=NULL/fit=NULL → 对 SUM 零贡献)。见末节「指纹漂移诚实记录」。
> 域规模速览:KOL Pool 113 端点 · Projects 45 · Intelligence 53 · Events 11 · Attribution/Dashboard 47 · 基建 1(库/队列/网关)。

---

## 〇、域骨架速览(三件产出汇总)

| 域 | 端点 | 写的主表 | 触发 job_type | 外部依赖 |
|---|--:|---|---|---|
| **路1 KOL Pool** | 113 | vkpi_kol_pool(唯一 fit 写点 enrich_item pool.py:892)/favorites/evidence/sessions/claims | smart_search_profile_advance·session_advance·kol_profile_deep_crawl·kol_pool_comments_collect·kol_outreach_draft·account_dossier_extract·video | Apify/YouTube-V3/Gemini/llm_gateway |
| **路2 Projects** | 45 | vkpi_projects/contracts/assignments/cost_ledger/analysis_cache | project_contract_extract·contract_invoice_extract·contract_polish·project_retrospective_aggregate·logistics_track_sync | Claude/llm_gateway/17track/R2/Apify |
| **路3 Intelligence** | 53 | bh_products/competitor_signals/brand_signal/viltrox_matrix | (经 app.state.job_queue 入平台采集 job) | Apify×多/Claude/OpenAI/Gemini/RSS |
| **路4 Events** | 11 | activities/activity_events/activity_attributions | 无 | **无**(纯 DB 归因账本) |
| **路5 Attr/Dash** | 47 | sales_attributions(唯一账本 revenue.py:152)/shopify快照/对账队列/报表 | 无(对账走 DB 表非 apify_jobs) | Shopify/llm_gateway |
| **路6 基建** | 1 | schema_migrations/apify_jobs/job_execution_ledger/llm_calls/ai_cost_ledger | account_dossier_extract(followup自触) | OpenAI/Gemini/Claude/PG/Redis/Sentry |

---

## 图A · 域间依赖矩阵(谁调谁/读谁的表)

行=调用方,列=被调方。`fn`=调函数 `tbl`=读/写表 `job`=共享队列。**重点看循环。**

| ↓调用 \ 被调→ | KOLPool | Projects | Intel | Events | Attr/Dash | 基建/access/audit/costs |
|---|---|---|---|---|---|---|
| **KOLPool** | — | fn+tbl(workflow/projects表) | **fn(gemini_preflight/ai_brief/card/competitor)** | — | tbl(links/clicks/attrib 经 vkpi_kol_links 代理) | get_conn·scope·audit·budget_guard·tasks/queue·comments·products·recommendations·memory |
| **Projects** | **fn(auto_release_claims)** | — | — | — | tbl(读 sales_attrib/links) | costs.add_cost/void·scope·analysis.cache·media.r2·llm_gateway·audit·content |
| **Intel** | **fn(competitor_text/gemini_preflight)+tbl(读kol_pool)** | — | — | — | — | memory(Via整套)·staff(student_identity整套)·costs·audit·trends·settings·sync·products |
| **Events** | — | — | — | — | — | access(users)·audit(record_admin_action)·settings(rate_limit) |
| **Attr/Dash** | tbl(读kol_pool/favorites/assignments) | **fn(workflow.summary/list_projects)+tbl** | fn(brand_signal/matrix) | — | — | lineage·alerts·staff/kpi·costs·channels·comments·sync·scope·audit |
| **基建(worker/网关/cron)** | fn(dossier/final_v1/profile_discovery) | fn(workflow.staff_id) | — | — | — | costs.budget_guard·sync.cron·access.scope·channels·logistics·audit |
| **入站回写→Events** | — | — | — | ← access(auth record_registration)/audit(record_submission)/operations(orders record_purchase) | — | — |

**循环依赖(标红,收尾期慎动)**:
- 🔴 **KOLPool ↔ Intelligence**:KOL 调 intel 的 gemini_preflight/ai_brief/competitor;intel 的 brand_signal_detector 反读 vkpi_kol_pool + 复用 kol.competitor_text。双向缠绕。
- 🔴 **KOLPool ↔ Projects**:projects 调 kol.auto_release_claims_for_project;kol 读写 vkpi_projects/assignments(C9 funnel)。
- 🔴 **Attr/Dash ↔ Projects**:attribution 读 project 表 + 调 workflow.summary;projects 读 sales_attributions/links。
- 🟢 **单向健康**:所有域 → 基建(get_conn/scope/audit/llm_gateway/budget_guard);所有写域 → apify_jobs。这是正常的"调用心脏",非病态环。

---

## 图B · 共享资源热点("改一处崩多处"雷区)

| 资源 | 类型 | 谁在用 | 风险 |
|---|---|---|---|
| **get_conn / db_connection_scope**(connection.py:628/588) | 函数 | **270 文件 import,全 40 域** | 🔴绝对心脏。ContextVar 作用域决定连接复用,worker 调 domain 函数误用作用域=污染整批(已知坑,须 db_connection_sync_scope 包裹) |
| **apify_jobs** | 表 | **11+ 域写入**,worker 独占消费,tasks/queue_view 读画泳道 | 🔴单一串行队列;新 job_type 三点同步(worker dispatch / queue_view infer / 泳道)漏一处即哑 |
| **scope**(access 域:assert_project_access/can_view_all/row_staff_filter) | 函数 | 全域权限基座,几乎每个读写点 | 🔴权限语义改一处影响全站可见性 |
| **llm_gateway.invoke**(platform:458) | 函数 | **22 文件共享**统一 LLM 出口 | 🔴provider 回退顺序+预算策略一处改影响全域 AI(见图C 护栏) |
| **vkpi_analysis_cache** | 表 | video/outreach/dossier/contract/invoice/polish/retrospective 七种 worker 产物 + kol/intel/projects 读 | 🟡derive_method 命名空间共享,status CHECK 仅 ready/stale |
| **vkpi_kol_pool** | 表 | KOL 域主表(1134),被 search/recommendations/intel/projects/attribution 读 | 🟡读侧广,viltrox_fit_score 写点单一(enrich_item)且全域护栏(快照回滚) |
| **kols**(主表) | 表 | **3 条独立写路径**:promote_to_main / claim_store:44 / kol_ops | 🟡多写路径,口径需一致 |
| **vkpi_cost_ledger** | 表 | costs 域主表,但 Projects(contracts/workflow)直接 INSERT/UPDATE cash_fee | 🟡跨域直写,靠 source_ref 幂等去重 |
| **vkpi_sales_attributions** | 表 | attribution 唯一账本(revenue.py:152),被 lineage/staff/kpi 读 | 🟢单一写点,append-only 负额冲销 |
| **vkpi_llm_calls / vkpi_provider_budget_caps / vkpi_ai_cost_ledger** | 表 | llm_gateway 写、budget_guard 写、queue_view/worker 读 | 🟡AI 用量与预算总账,单一写点 |
| **staff/users + staff_context_for_user** | 表+函数 | 全 RBAC,43 router + 全中间件 | 🔴认证基座 |

---

## 图C · API 命脉全账(8 条核对 + 揪漏 10 条 + 护栏总盘)

### C-1 已知 8 条(全部坐实,call point + env)
| 服务 | 用途 | 调用点 | env |
|---|---|---|---|
| Anthropic Claude | 合同/视觉/周报 LLM | claude_client.py:27 / llm_gateway.py:48 | ANTHROPIC_API_KEY |
| OpenAI | 主链 + embedding | profile_recall.py:93 / llm_gateway.py:34 | OPENAI_API_KEY |
| Google Gemini | 视频分析/LLM | gemini_client.py:14 / llm_gateway.py:41 | GEMINI_API_KEY/GOOGLE_API_KEY |
| Apify | 各平台爬虫 | apify_batch_refresh.py:556 / industry_crawlers/* | APIFY_TOKEN(+PREVIOUS) |
| Decodo | yt-dlp 出口代理 | ytdlp.py:49 / config.py:378 | YTDLP_PROXY |
| YouTube Data API v3 | 频道/视频元数据 | youtube_crawler.py:41 / workflow_evidence.py:284 | YOUTUBE_API_KEY |
| Cloudflare R2 | 对象存储 | r2.py:23(唯一 boto3) | R2_ENDPOINT/KEY/SECRET/BUCKET |
| 17track | 物流轨迹 | seventeen_track.py:23/75 | VKPI_17TRACK_TOKEN(逗号池) |

### C-2 揪漏 10 条(8 条之外,新增)
| 服务 | 类别 | 状态 | 调用点 |
|---|---|---|---|
| **SMTP(smtplib)** | 出站邮件 | **活跃**(注册/改密真发,缺配降级 DEV 日志) | auth/email.py:34 |
| Resend | 邮件 SaaS | 半接(有 key+健康探针,实发走 SMTP) | provider_health.py:108 |
| **Shopify webhook** | 电商入站 | **活跃**(HMAC 校验+订单归因) | webhooks.py:280 |
| Meta/IG webhook | 平台入站 | 默认 not_configured | webhooks.py:295 |
| TikTok webhook | 平台入站 | 默认 not_configured | webhooks.py:309 |
| **X/Twitter API v2** | 平台直采 | 直采优先,无 token 回退 Apify | x_crawler.py:32 |
| **Reddit PRAW** | 平台直采 | 直采优先,回退 Apify | reddit_crawler.py:41 |
| **Qdrant** | 向量库 SaaS | 活跃(VIA记忆/KOL召回,可本地回退) | vector_memory.py:12 |
| Sentry | 错误追踪 | 有 DSN 才启用 | main.py:477 |
| **Redis** | 缓存/队列基建 | **生产必需** | jobs/queue.py / rate_limiter.py |

**死配置/占位**:RapidAPI(config.py:114 声明零使用)、PayPal/Stripe(登记表占位未接)。**未发现**:飞书/Slack/钉钉/Twilio/SendGrid/翻译/Maps。

### C-3 护栏总盘(按量计费)——**放量判定:否(P0 不可放量)**
预算 cap **已在生产库播种**(monthly_total $300 / gemini $200 / openai $100 / apify $40 / claude $100 / single_call $0.50),current_spend 在累计(monthly $62.45)。**问题:cap 在,enforcement 被显式关成 telemetry。**

| 服务 | 速率/熔断 | 预算闸 | 超限行为 | 判 |
|---|---|---|---|---|
| Gemini | 部分(仅 guarded_provider_call 经熔断+信号量;invoke/worker 主线不经) | record-only(主线失效);P4 单KOL线真拦 | 主线放行 | 🟡半护 |
| OpenAI | embedding **零护栏**(profile_recall.py:105 / vector_memory.py:150 整批无上限) | 路径不查 | 放行 | 🔴裸奔 |
| Apify | **零熔断/零并发/零记账**,provider:apify spend 恒 $0 | 死配置(从不记账) | 放行 | 🔴最危险 |
| 17track | 6h冷却/LIMIT80/签收跳过/token轮换 | 无(免费池,量级节流) | 换 token | 🟢唯一到位 |
| Decodo | 仅 max_posts 条数 | 无 scope | 放行 | 🔴按量则裸奔 |

**唯一真硬闸**:合同抽取 contracts.py:545(`if not budget.allowed: raise budget_guard_blocked`)——证明 budget_guard **能**硬拦,其余是**主动选 record-only**。
**放量前必修(P0)**:① llm_gateway.py:536-550 provider/monthly 预算超限照发 → 改硬拦或熔断;② apify_jobs_worker.py:1648 预检 "would block, continuing";③ OpenAI embedding 补护栏+批量上限;④ Apify actor 调用补预算记账+预检(provider:apify cap 形同虚设);⑤ Decodo 无护栏。

### C-4 三特查(Decodo)
- **角色**:Decodo = `YTDLP_PROXY`,**专供 yt-dlp 子进程出口**(视频字节下载/字幕/元数据),与 Apify 自带住宅代理(`useApifyProxy`)**两套独立**。注入点 7 处(gemini_video.py:1655 核心慢路径 / 关键帧 QA / ytdlp 元数据等)。
- **ISP 中断影响**:🔴瘫**非 YouTube 视频分析**(TikTok/Bili/IG 走 ytdlp 下载)+ **关键帧 QA**;🟡YouTube 降级(fast path 用 Gemini 直传 URL 不下载,仅 fallback 受影响);🟢**Apify 抓取链零影响**(独立代理)。
- **vs Apify 合并省 $81/月**:**不能简单合并**——Apify 交付结构化 JSON(`shouldDownloadVideos:False`),Decodo 交付**视频字节文件**喂 Gemini/Claude 看帧,本质不同。可省的前提是**收缩 Decodo 使用面**:把元数据(发布日期/指标刷新)切 Apify(功能已存在)+ 依赖 YouTube fast path,剩余非 YouTube 字节下载若改 Gemini 直传或换更便宜代理,则 Decodo 可下线。**未迁移前硬关 = 牺牲非 YouTube 视频分析,不建议。**
- **死代码**:sse.py:20 `YTDLP_PROXY` import 后未用。

---

## 图D · 数据流命脉(KOL 建档 → 列表显示)

**穿 4 表 + 3 job,一次深爬最多 5 次写入**(HTTP 同步入口与 worker 异步入口共用 `_execute_profile_flow` 内核 url_deep_crawl.py:616):

```
入队 apify_jobs(kol_profile_deep_crawl) → worker claim(FOR UPDATE SKIP LOCKED :3110)
  → _execute_profile_flow:616
    ① 爬虫抓取(Apify/yt-dlp,仅读外部)
    ② write_kol_profile_basics → INSERT/UPDATE vkpi_kol_pool(profile_basics.py:224,不碰 fit 分,前后比分回滚护栏)
    ③ _enqueue_final_v1 → INSERT apify_jobs(job_type=video)  ──┐
    ④ ensure_video_evidence → INSERT vkpi_kol_video_evidence(video_evidence.py:191)
    ⑤ _enqueue_account_dossier_extract_followup → INSERT apify_jobs(account_dossier_extract)
    ⑥ _record_deep_crawl_run → INSERT vkpi_kol_url_deep_crawl_runs
  → 下游 video job 析完回写 deep_result
显示:GET /kol-pool → list_pool(pool.py:231/271)读 vkpi_kol_pool
```
DB 实测:vkpi_kol_pool 1134 行 / vkpi_kol_video_evidence 1309 行。

### 两根因写入点(实测复核)
- **根因① 458「假增量」**【成立·结构性】:`url_deep_crawl.py:651` crawl 调用**漏传 incremental_state**;`_crawl_profile_basics`(:1890)及各平台爬虫签名**只有 max_posts、无 since/after/游标**(instagram_crawler.py:164/200,Apify input 仅 resultsLimit)。incremental_state 仅在 `_filter_incremental_profile_videos`(:1451)做**全量爬回后的客户端裁剪** → 每次按 max_posts 全量拉、内存丢旧、`skipped_by_incremental` 是"假装跳过"。**配额一分没省。**(归 E5/E6 施工窗,候"施工"字)
- **根因② evidence.project_id 落空**【成立·实测 503 NULL,非 417,已增长】:`vkpi_kol_video_evidence` 唯二 INSERT——路径A(workflow_evidence.py:505 项目提交,**带 project_id**)/ 路径B(video_evidence.py:153-181 深爬历史物化,**values 字典无 project_id key**,列从 values 派生 → 落 NULL)。**架构必然**:路径B 在建档期触发,此刻 KOL 尚未分配任何 project,无 project_id 可填——它是 kol_pool 维度证据非 project 维度。NULL 全集中深爬路径(manual_url 301/apify 122/youtube_api 80),legacy 导入 0 NULL。**非 bug,是语义分层**——若要补 project 归属须在 KOL 进项目时回填,不是写入点漏填。

---

## 架构收尾判定(三档)

### 🟢 健康穿插(共享基建,正常,不动)
- get_conn/db_scope、scope(access 权限)、audit 留痕、llm_gateway 统一出口、budget_guard.record_cost、apify_jobs 单队列(设计如此)、vkpi_llm_calls/budget 账本、staff/users RBAC——这些是"被所有域调用的心脏",单向依赖,健康。
- 合同费 ↔ 成本账本双向耦合(confirm 写 cash_fee / delete void / GEN 回填,source_ref 幂等)——真实且自洽。
- evidence 路径B 无 project_id——语义分层正确(kol_pool 维度 vs project 维度),非缺陷。
- Events 域纯 DB 归因账本(无 job/LLM/外部),三跨域回写点全 best-effort try/except——干净。

### 🟡 该解耦的债(收尾期不动,记 Codex)
- **域边界模糊**:`/api/admin/intel` 挂着 Via(memory 域)+ student_identity(staff 域)整套;`vkpi_industry_automation` 把 audience/experiments/industry/launch/predictions/trends/products/recommendations/market **9 个域混在一个 router 文件**。
- **目录 ≠ 路由归属**:`domains/projects/p5_selected.py`(campaigns/budget_pools)被 operations 域独占挂载,不属 projects API。
- **表面 ≠ 实质**:`vkpi_kol_links` 的 /links/* 全代理 attribution 域(KOL 路由表面、attribution 实质)。
- **kols 主表 3 条独立写路径**(promote/claim_store/kol_ops)口径需收口。
- **双前缀分裂**:`/api/admin/vkpi/kol-pool`(新池 1134 真数据) vs `/api/admin/kol`(kol_ops 旧表)。
- **缺表静默降级**:`ai_insights`、`correlation_cache` DB 中 **MISSING**,gaps/insights/correlations 端点静默返空(疑未迁移)。
- **死代码/配置**:sse.py:20 YTDLP_PROXY 未用、RapidAPI 死配置、PayPal/Stripe 占位。

### 🔴 会反咬的收尾风险(放量前必知)
1. **护栏神经断联(P0)**:llm_gateway.invoke + apify worker 全局 record-only;monthly/provider cap 在主流量**不拒绝**,Apify 花费**完全不可见**(spend 恒 $0)。**放量 = 月度/单 provider 上限不可执行。** 放量前必修图C-3 五点。
2. **OpenAI embedding 零护栏整批无上限(P0)**:profile_recall.py:105 / vector_memory.py:150,最易高频按量点裸奔。
3. **dashboard 4 端点真假穿插**:agents/copilot-brief/tasks/inbox 数据源是磁盘 `runtime/ops/*.json` 工件(filesystem glob)却返 `is_real=True`——放量前要么接真要么标占位。
4. **get_conn ContextVar 作用域**:worker 调 domain 函数误用作用域污染整批(已知坑,须 db_connection_sync_scope)。
5. **458 假增量烧配额 + Decodo 无护栏**:放量放大外部成本(归 E5/E6)。

---

## 附:实跑纪律 + 指纹漂移诚实记录
**审计纯只读** X 光,零写库、零 commit、零入队;唯一 DB 接触为只读 SELECT/information_schema 核表。

**指纹漂移(第六起合法漂移,如实记)**:测绘窗口内 `vkpi_kol_pool` **1128 → 1134**(+6 行),SUM(fit_score) **32828.7260 守恒**、scored 999 不变。
- **出处**:**非本次审计**(审计零写)。是先前**探针5 命门实跑**那条废数据 `smart_search_profile_advance`(job 944)的级联仍在异步后台运行——管线持续发现新 TikTok/YouTube 创作者并 INSERT 进池(id 5668-5673,`source_type=manual`/`created_by_staff_id=NULL`/`viltrox_fit_score=NULL`)。fit 全 NULL → 对 SUM 零贡献,评分集冻结未破;`reason=''` 空串使 COUNT(reason) 同步 +6。
- **现场即图C-3 风险实证**:一条测试查询触发了无界级联(job 944+ 一度 done14/queued12),正是本图「护栏 record-only、Apify spend 不可见」P0 的活体演示。
- **处置**:尝试掐断排队级联被 auto-mode 正确拦截(越过应用路径写共享队列、未经授权)——未强行绕过;~13 个 job 自然 drain,6 行已入池(无害,NULL fit)。**候 Jianbo 定夺**是否清掉这批废查询级联产物。
