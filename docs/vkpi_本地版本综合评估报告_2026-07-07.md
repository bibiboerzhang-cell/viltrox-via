# V-KPI 本地版本综合评估报告

日期: 2026-07-07 | 基线: `29d3b5277` / codex/dashboard-real(W1-W6 已落,本地栈 8102 sha 对齐)
方法: 七路并行评估(五路只读代码审计 + 两路外部深度研究,均带文件:行号/URL 证据)

## 0. 总评分卡

| 维度 | 分 | 一句话 |
|---|---|---|
| 后端代码 | 74 | 架构纪律 80+ 水位,被"闸当前红/dashboard 隐藏写/测试没碰真 PG"拖下来 |
| 前端代码+UI | 67 | 动效纪律与工程护栏强,但 90% 页面是死的、数据层裸 fetch 零缓存、全仓 0 memo |
| 小组模式 | 57 | 分组/权限/在线状态地基 80 分,"组作为协作单元"基本为零 |
| 智能体/推论 | 60 | 循环真实但感知浅;区间预测有活体但校准链断头;W6 账本有表无水 |
| 上线就绪 | 就绪(带 15 步剧本) | 62 提交领先线上,12 迁移全 additive 自动 apply,8 个 env 键要手补 |
| (参照)全系统审计均值 | 78.7 | 拖分项仍是真数据缺,非代码缺 |

## 1. 本次评估抓到的新实锤(按严重度)

1. **verify 发布闸当前是红的**:STEP4 硬化闸 4 个 silent-exception FAIL(indie_site_actions.py:96/131、rule_validation.py:185、strategy_sim.py:210——W3/W4 波欠的债)+ STEP5.5 千行卫兵 FAIL(GtmCommandPage.tsx 1003 行,W5c 加代码后超标)。**上云前必须修绿,verify 是发车门。**
2. **W1 时区 bug**:`job_vkpi_bet_review_due` 的 CronTrigger 漏了 `timezone=CHINA_TZ`(jobs.py:286),同文件其他 job 全带;线上服务器 UTC 则实际 14:00 CN 才跑,与 06:10 裁决生成错位半天。
3. **C2-C4 协作波规格文档是幻影**:`docs/vkpi_协作波实施规格_团队互通_2026-07-07.md` 工作区无、git log --all 无——是工具故障期记忆引用的从未落盘文件。协作波施工前必须先重写规格。
4. **双认领协议毒化风险**:local_workers 租约只打 payload 标记不改 status(registry.py:265),主 worker 认领 SQL 不排除租约标记(apify_jobs_worker.py:399-416)→ 会抢走本地算力任务并把未知类型烧到 MAX_ATTEMPTS。**Tauri 本地算力上线前必修。**
5. **GET /dashboard 是隐藏写者+写侧 N+1**:每次页面加载 lineage 真插表+逐 metric/逐 source 单条 INSERT,单请求约 25-40 条串行查询,全链零缓存(lineage/service.py:118-129, store.py:41-44)。与"总脑接口纯读"红线同族问题。
6. **潜伏 500**:dashboard handler 查询参数 `scope: str` 遮蔽模块 import scope(vkpi_dashboard_staff.py:242 vs :12),ScopeDenied 一旦真抛会变 AttributeError→500 而非 403。
7. **静默异常真实存量是闸口径的 100 倍**:AST 全量 3146 个 except 中 628 个完全静默(闸只查窄口径故只报 4)。治理盯宽口径,闸守棘轮。
8. **预测对答案链断头**:vkpi_forecast_log 1656 行仅 2 条已裁决,refresh_forecast_outcomes 只有手动 HTTP 入口未挂 job(注释自认"每日 job 后续")。
9. **skill 账本被测试污染**:vkpi_skill_runs 264 行中约 220 行是 pytest 的 test_skill_* 污染,未来校准统计会被带歪。
10. **dist 赘肉**:7.5MB 演示 PDF 占 dist 62%;两个 app chunk 贴着 600KB 闸线;leaflet eager preload 非首屏必需。
11. **第 4 份 manager 闸复制体**:vkpi_staff_groups.py:18-28 未收编进 W5a 的 manager_guard。
12. **本地 worker 心跳停在 07-03**(8102 web 活着但 worker 离线 4 天,/health trust 块诚实暴露)。

## 2. 本地→线上(第 7 轮上云)

**领先量**:本地 HEAD 领先 7-03 收敛锚点 `336cc665a` 共 62 提交(36 代码+26 文档);线上 db_migration_max=209,待 apply 210-221 共 12 条(全 additive,已全部登记 connection.py,web 重启自动跑,无 `?` 陷阱)。

**两个回填脚本(已确认实物)**:
- `scripts/backfill_apify_raw.py` — B1 解析债五阶段回填(emails/commenters/languages/media/contact_snapshot),依赖迁移 214,幂等只补空,先 --dry-run;
- `scripts/seed_kol_rates_from_contracts.py` — 从 confirmed 合同费用种报价库,依赖迁移 211,加 --write 落库。不跑则报价全走行业基准兜底。

**8 个 env 键**(.env 不随 rsync,历史有 LLM 全挡事故):LLM_MONTHLY_BUDGET_USD(必查>0)/ ENABLE_SCHEDULER=1(且 --workers 1 防双跑)/ **KOL_DISCOVERY_BUILDOUT 首日置 0**(默认 ON=搜索即点火 Apify 深爬,余额仅 $3.4,7/8 重置后再开)/ VKPI_LOCAL_WORKER_SECRET(可后置)/ VKPI_MONTHLY_SPEND_ALERT_USD / VKPI_OWN_BRAND_HANDLE_PREFIXES / 车道参数走第二 EnvironmentFile / 反查 OPS_SCHEDULER_FORCE_ENABLE 未设。

**15 步部署剧本**(完整版见评估原文,骨架):verify 修绿发车 → curl 线上 /health 取真基线 + rsync -n 干跑核对删除项 → 手补 env → 本地 npm ci && build + 盖 BUILD_GIT_SHA → 低峰窗备份(tar+pg_dump+dist.bak)→ 停 timer/worker(pkill 用 `app[.]workers` 防自杀)→ rsync 四目录排除 .env/runtime/uploads → .venv import 全链验证 → 全量重启轮询 /health 至 sha=29d3b5277 且 db_migration_max=221 → 重放回填①(迁移 214 后)→ 回填②(迁移 211 后)→ 起 worker+timer+pip -U yt-dlp → 冒烟(登录/Pool/GTM 页/Inbox/四敏感写口权限矩阵)→ **观察 24h 后**按 task_key 单开两个 GTM 闸(勿用 gray_release --tier low,会连带翻其它任务)→ 盯 1-2 个 cron 周期。

**最大风险**:W5a 权限闸上线即收紧——goaffpro 佣金/优惠码是既有线上功能,若现由非管理岗运营操作,上线当天 403;需提前在 Settings 矩阵授权或换人。GTM 两口是新功能影响面小。

## 3. UI 优化路线(前端 67 分)

**现状**:动效有成文宪法且执行到位(motion.css 63 行/入场变化各一次/reduced-motion 全降级),但覆盖极窄——147 个 page 文件仅 9 个含动画,33 个裸 table,tables/ 目录 8 组件零动画;数据层是最大短板(裸 fetch 无缓存无去重,切 tab=全量重拉+骨架闪);全仓 0 处 React.memo;152 文件 createElement 包袱。

**落地队列(全部不引重框架)**:
1. 【小】列表/详情 keepPreviousData:切 tab 不清旧数据、后台刷新原地替换,配 AnimatedNumber 数字补间——消除全站骨架闪,性价比第一刀;
2. 【中】apiFetch 最小缓存层(~100 行):in-flight 去重+TTL 30-60s+stale-while-revalidate,先覆盖 dashboard/进度中心/池列表高频 GET;(或直接上 TanStack Query v5,13kB,首刀只包 Dashboard+Inbox)
3. 【中】TableShell 复用件铺动效死区:骨架 shimmer+行 stagger 入场一次+数字列 AnimatedNumber,分批替换 8 静态表+33 裸 table;
4. 【中】SSE 扩面:复用 TaskCenter 的 EventSource 孤例,进度中心/任务队列改事件流,5s+10s+10s 三路轮询归一;
5. 【小】重渲染裁剪:轮询驱动的列表项加 React.memo(当前全仓 0 处);
6. 【小】LazyMotion 瘦身:framer-motion 43 文件全量 import 改 m+LazyMotion,34kB→4.6kB(外研:Motion 官方路径);
7. 【小】CSS 原生进出场三件套(@starting-style + allow-discrete,Baseline 2024-08):抽屉/toast/下拉零 JS 动效;
8. 【小】dist 减重:7.5MB PDF 移出 public/、删 manualChunks 死规则、leaflet 改懒加载;
9. 【大·分批】createElement→JSX codemod 152 文件(动效改造会碰的 27 个 pages 优先);
10. 【一次性变更高亮基元】row-flash ~30 行自研(Smashing 2025 实时仪表盘模式:soft pulse 替代硬切换)。

**外研裁决**:采纳 Vercel Web Interface Guidelines 为动效规范(CSS>WAAPI>JS 库,与现有宪法同向);观望 number-flow(与自研 AnimatedNumber 重叠)/View Transitions(React 18 需手包)/auto-animate(职责重复)/TanStack Virtual(列表均已截断无千行 DOM 实锤)/Web Worker(前端无重计算实锤);不采 Ollama 内嵌(吞吐仅 llama.cpp 直跑一半)、不引第二动效框架。

**本地运算判定**:客户端侧=先缓存层后 Worker(现无重计算场景);本地机器侧=外部实践与 7-04 Tauri 决策完全同向,增量结论:llama-server 用 JIT sidecar 下载(免 15MB 进包)、3-8B 小模型(Qwen 级)做评论语言/相关性预筛达 GPT-3.5 级且**顺带解掉 GFW 代理依赖**,边际成本 $0。周级工程,按九缺口施工单排期。

## 4. 后端修债清单(74 分)

亮点:worker 工程成熟(SKIP LOCKED 三档优先/断线重连/心跳/优雅 requeue)、KOL 池是读路径样板(单 SQL+300s 缓存+脱敏)、权限是测试第一公民(124 节点矩阵)、/health trust 块诚实。

| 债 | 量级 | 修法 |
|---|---|---|
| verify 修绿 | 小 | 4 处补 logger.warning + GtmCommandPage 按 Section 拆<1000 |
| 双认领毒化 | 小 | 主 worker WHERE 加 local_lease_id IS NULL + job_type 过滤 |
| dashboard 去写化+缓存 | 中 | generate_run 挪 cron;循环 INSERT 改 executemany;summary 套 60-300s 读缓存;顺修 scope 遮蔽 |
| 静默异常棘轮 | 中 | 先补 90 个 pass/continue 无日志;check_silent_exception_baseline 并入 verify 滚动递减 |
| 可观测性 | 中 | request metrics 5 分钟落库或 /metrics;SENTRY_DSN 进部署 env 清单;request_id 中间件 |
| PG 真库测试层 | 大 | CI 起一次性 PG:双 worker 并发抢单/翻译层全语句往返/JSONB 方言;pytest 加 --cov 建基线 |
| helper 收敛 | 中 | _text×109/_loads×70/_truthy×29/_int0×16 收敛 app/core/coerce.py,AST 对照保行为;translate_sql_dialect 加 lru_cache |

## 5. 小组模式(57 分)

**地基真实**:vkpi_staff_groups 单表+成员 jsonb,5 写口全有管理层闸;组共享是真展开(projects/kol_pool members 幂等铺行,删组自动撤);在线状态全链通(60s 心跳→last_seen_at→绿点);scope.py 零组感知——组是纯"宏",架构干净。

**协作单元为零**:无 IM/无内部评论/无 @mention 投递(reminder_rule 是死字段零消费方);Inbox 无组维(owner_staff_id 单值,scope 只有 own/all);Events 无组共享(team_ids 是个人数组,ShareModal 无组选择);无组长角色(管理权全收公司级);通知基建断头(偏好表在、无 feed 表无端点)。

**补全路线**:P0 重写 C2-C4 规格(幻影文档)+ staff_groups 闸收编 manager_guard → P1 Events 组共享(镜像既有幂等展开范式)+ Inbox 加 owner_group_id(C4 路由落点前提)→ P2 团队协作图页(数据源全齐纯读)+ 激活死字段(kpi_goal 接组聚合、reminder_rule 接每日 job→通知 feed 表+铃铛;16 人规模下比 IM 性价比高)→ P3 智能路由(从管理层手动派发起步)+ 实体级评论@(排真数据流之后)。

## 6. 智能体与推论(60 分)

**现状**:agent_cycle 是真链非空转(05:30 durable workflow→10 个 SQL 规则 producer→inbox 291 条,9 次 completed run),但感知面是运维杂务(failed_retry 占 127)且零 LLM 推理,瓶颈卡人审(13 executed vs 200 suggested);skill 编排有真调用方但名实差距大(creator_match 40 次真跑,其余 4 个 skill 合计 4 次);**区间预测已有活体**——evidence_quantile_v1 对 KOL 出 p10/p50/p90 已落 1656 行 forecast_log,但 1654 条 pending 没人对答案;W6 校准零件齐全但 0 调用方;周序列数据不够(19865 行只有 24 个快照日,断更 3 周);SKU 销售靶子要等 W8。

**推论施工顺序**:
- **立刻做(四小刀,不等任何前置)**:①refresh_forecast_outcomes 挂每日 job(1656 条开始自动对答案,唯一能马上积累区间覆盖率的真数据源)②evidence_quantile_v1 双写进 W6 prediction_runs+挂每周 rollup job→首份周评估报告不必等新模型 ③清 skill_runs 测试污染(测试落账走独立 flag)④用 24 天日增量做"经验分位数+naive 漂移"日基线写 prediction_runs,先让账本流水转起来;
- **等数据(墙钟)**:线上 sync 恢复灌 channel_post_metrics 并回补本地;周序列≥8-12 点约到 9 月才值得上 StatsForecast;真 bet 补到 ≥20 让 gtm_outcomes 首批裁决落地;
- **等外部件**:W8 shpat_ 打通后 SKU 销售预测才有 actual,此前靶子只能是 views/engagement(明示口径避免虚假承诺);
- **后排**:bandit 严守硬门"W9 评估在线才放权";先做 FEEDBACK_SCORE 按周评估衰减/放大的轻量版。

**外研模式采纳**(全部纸面模式,1-4 人日/件,总增量 6-9 人日):工具风险分级+审批闸(low/med/high 三维,high 走既有发布审批)/ 委派四要素+effort 分级写进 orchestrator / eval 三件(20 条黄金查询+LLM-judge end-state 打分+生产回归自动转永久 case)/ **promptfoo** 自托管接 Eval 雏形(MIT、100% 本地、Python provider 可包代理 LLM;pin 版本防 OpenAI 收编后漂移)/ **Bet Ledger→FVA 账本**(补基线预测值+来源步骤两字段,量化"模型比拍脑袋强多少")/ 每周校准回路(评估必跑、重训稀疏、MAPIE 校准集每周用最新残差刷新)/ **Evidently 纯库模式**(cron 跑 Report→JSON 落自家 PG+静态 HTML,零新服务)/ 人=tool call(HITL 统一进工具机制,复用 durable 断点)。观望:Letta 记忆分层/MLflow(与 Bet Ledger 重叠)/Langfuse(四件套过重)。不采:任何 agent 框架本体(Anthropic《Building Effective Agents》+12-Factor 双重背书自建路线)。

## 7. 综合行动队列(评估结论→波次)

| 波 | 内容 | 量级 | 前置 |
|---|---|---|---|
| **W6.5 修债刀** | verify 修绿(4 静默+拆 GtmCommandPage)+ 时区 bug + scope 遮蔽 + 双认领过滤 + staff_groups 闸收编 + dist PDF 移出 + skill_runs 污染清理 | 1 天 | 无,立即 |
| **推论点火四小刀** | forecast_outcomes 挂 job + 双写 prediction_runs + 周 rollup job + 经验分位数日基线 | 1-2 天 | 无,立即 |
| **W7 上云** | 15 步剧本 | 半天+24h 观察 | verify 绿 + 用户发令 + env 清单 |
| **UI 动态第一批** | keepPreviousData + 缓存层 + row-flash + LazyMotion + dist 减重 | 2-3 天 | 无 |
| **C2-C4 前置** | 重写协作规格入 docs | 半天 | 无 |
| **UI 第二批** | TableShell 铺开 + SSE 扩面 + memo | 3-4 天 | 第一批 |
| **协作波 C2-C4** | Events 组共享→Inbox 组维→协作图→通知 feed | 波级 | 规格+读裁决 |
| **W9 评估在线** | FVA 账本 + promptfoo + Evidently 纯库 + 每周校准 | 6-9 人日 | 推论四小刀 |
| **W10 放权** | bandit-lite + 人=tool call + 工具风险分级 | 波级 | W9 硬门 |
| **Tauri 本地算力** | sidecar JIT + 双认领修复前置 | 周级 | 九缺口施工单 |

## 8. 一句话总评

代码质量整体在 70-80 分位、护栏文化是真资产;拖后腿的三样全是"接线和水"——verify 闸红着(接线债)、预测没人对答案(账本没水)、协作单元没建(小组只有地基)。修债刀+推论四小刀共 2-3 天就能把 60 分档的两个维度抬进 70+,上云剧本已备齐随时发车。
