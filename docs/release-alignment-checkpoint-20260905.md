# 版本对齐检查点（2026-09-05）

## 结论

本轮推进发布前修复和隔离验证；尚未重启本地、切换云端或迁移业务库。
不得把源码推送、定向测试或合成数据库演练表述为运行版本已经一致。

## 已核实的运行状态

- 源码起点：`9a6f17c1745c012cb21e8569bb70d4a330a3293b`。
- 本地：服务 `87192cf6`，前端 `f845fb07`；健康状态 degraded，部分 Worker SHA 不一致。
- 云端：服务、前端及 Worker 均为旧 `b5c9d0ff`；健康状态 ok。
- 云端完整迁移集合：已应用 301 项，当前清单 305 项；待执行仅 307–310，无额外未知版本。
- 云端旧 `kol_profile_incremental_refresh` 元数据开关为 true；旧代码未执行该任务链。310 会强制关闭，重新启用必须另行确认。
- 云端未完成 Anthropic 批次为 0。本地 all-scope 活动任务、未知费用边界和 Redis pending/lag 均为 0；此为时间点观察，不是之后发布时可复用的排水证明。

## 本地历史迁移差异：保留，不删台账

本地已应用 306 项，缺少 310，同时多出当前仓库不存在的两个名称：

- `081_vkpi_repair_center_persistence.sql`
- `083_vkpi_repair_future_write_mapping.sql`

历史线索分别位于 `runtime/ops/20260524T132718Z-repair-proposal-first-write-smoke.md`
和 `runtime/ops/20260524T150233Z-repair-future-write-mapping-first-write-smoke.md`，
均记录了当时局部首写及备份位置。它们不是可以直接清除的无来源记录。
当前 canonical pending helper 会拒绝该集合；须审定历史兼容口径，不能伪造缺失迁移内容或改写历史。

## 本轮修复与新增验证

- 修复 7 个 GitHub 失败用例的 CI 专用环境变量污染，只清理这些受测子进程的环境；生产部署禁止该变量的守卫保持不变。
- 3 个受影响文件加 canonical release gate 定向检查：51 项通过。
- 新增敏感真实路由与发布围栏联装测试；新增文件 64 项通过，8 个相关围栏文件共 217 项通过。它只证明 HTTP 准入和零副作用边界，不证明真实平台或迁移回退完成。
- 新增 307–310 合成 PostgreSQL 演练工具：默认只打印计划；必须显式指定独占私有临时集群后才能执行。业务数据库和供应商均不在执行范围内。
- 合并运行上述 12 个发布/围栏测试文件：268 项通过、2 项既有弃用警告。
- 演练工具自身的离线回归：44 项通过，使用假连接验证隔离身份、事务、清理及脱敏合同；与真实 PostgreSQL 演练分开计证。
- PostgreSQL 16.13 合成实测通过：306/307 两个起点均升级到 310；7 个故障注入点均整批回滚；原有合成行保留；迁移台账精确匹配 305 项。每场 8 个连接共尝试 40 次，仅分配 5 个当日槽位；重放、跨日和外键行为通过。
- 两个合成数据库已删除，独立查询确认剩余非标准数据库为 0，临时实例已关闭。证据：`runtime/ops/migrations-307-310-rehearsal-20260905.json`。这不证明旧应用/新浏览器会话回退兼容。

## 后续发布仍需满足的条件

1. 目标绑定的新备份及恢复证据；不能复用旧迁移 273 的备份作为本次升级证明。
2. 本地有明确的维护/排水/围栏编排。现有 supervisor 会补起 Web、scheduler 和 Worker；直接重启可能触发迁移和旧任务，不能假设是纯换代码。
3. 为 308–310 审查独立、精确的发布策略；不得冒充既有 nullable/defaultless/no-row-writes 白名单。
4. 验证旧应用与新 schema、门户 token、cookie 会话和浏览器缓存的兼容边界。新前端会清旧 localStorage token，数据库零写不等于客户端会话无变化。
5. 明确维护窗口及不可逆激活点后的前向恢复安排。现有发布脚本在移除围栏之前设置提交标志；之后不可假装通过回旧应用撤销业务写入或外部费用。
6. 冻结最终 SHA，跑完整 canonical gate、CI、运行态和浏览器验收，再核对实际服务、前端、Worker 与完整迁移集合。

本检查点不授权删除历史、回灌业务库、放宽发布守卫或重新启用付费日抓取。
