"""market_brain — GTM 总脑纯读聚合域(GTM-1)。

显示层宪法(蓝图 v0.2 第五章):聪明藏在系统里,结果露在路线图上。
本包只做既有纯读函数的聚合编排,红线:零写库、零 LLM、零采集、
零复用带副作用的 GET(marketing-brain/daily 与 market/trends 一律不碰)。
响应只出 public_plan;private_evidence(评分明细/原始评论/竞品笔记/
模型特征/调试轨迹)结构有定义但默认绝不返回。
"""
