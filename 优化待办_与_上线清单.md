# V-KPI 优化待办 + 上线清单(2026-06-15 取证版)

> 来源:对 14 点升级蓝图逐条**对真代码取证**(7 agent / 480k tokens)+ 7 顶层页渲染冒烟。
> 结论:蓝图 ~65% 已落地(W0-W5 + P0-P5),**真实增益高度集中在点4、点8 两刀**。
> 本文件 = 上线后照着做的待办;按「价值÷难度」排序,别照蓝图从头新建(会重复造)。

---

## 一、上线清单(这一轮 → 服务器)

### 起飞前自检(已全绿 ✅)
| 项 | 状态 |
|---|---|
| 后端 pytest | 619 passed |
| 前端 vitest(含 7 页渲染冒烟)| 220 passed / 26 files |
| tsc / build | 0 错 / OK |
| 指纹 SUM(viltrox_fit_score) | 32828.726 不漂 |
| git 树 | 干净,HEAD `d4da79a1` |

### 上传步骤
1. 同步代码到服务器(`git pull` 或上传),前端 `npm run build` 出新 dist。
2. **跑迁移 142→147**(序列尾应为 `147_vkpi_kol_portal_tokens`)。
3. 服务器 env 增加:`PUBLIC_BASE_URL=https://www.viltroxvia.com`(webhook 注册要用)。
4. **重启 backend + worker**(worker 用 `.venv/bin/python`,别用裸 python3——缺 boto3)。
5. 验证 `/health`:`sha_aligned=true` · `worker_online=true` · `db_migration_max=147`。
6. 浏览器烟测 8 处:登录 / Dashboard / KOL Pool 搜索 / KOL 详情 / Projects / Events / Settings / Shopify Hub。
7. 等同事给 **Shopify `shpat_` token + store `.myshopify.com` 域名** → 填进 Hub「连接」区(已有 `shpss_` 加密密钥)→ webhook 自动注册到 viltroxvia.com。

### 红线(绝不做)
- **别轮换 `JWT_SECRET`** —— 所有 Fernet 加密 creds(18 官号 + Shopify)都解不开。
- 别写 `viltrox_fit_score` / 别改 `rule_v0`(指纹必须恒为 32828.726)。
- 密钥轮换 H2 再说(`shpss_` 已露过,可在 Shopify 后台点「轮换」换新再填)。

---

## 二、优化待办(上线跑起真数据后,按这个顺序)

### 🥇 Tier 1 — 大杠杆(~30% 工作量拿 ~70% 增益)
- **点4 履约闭环打通** `[L]` —— **唯一能把审计 62→89 的杠杆**。
  - 现状割裂:`seventeen_track.py:260` 只写 `assignment.metadata_json->shipping`,**不落** `vkpi_shipments.delivered_at`、**不推** `assignment.stage`。
  - 一刀:交付事件同时 upsert `vkpi_shipments.delivered_at` + 写回 `assignment.stage=delivered` → 观察窗(`observation_windows.py`)/ content_posts(表已存在 `migrations/129`)/ 复盘整链立刻有数据。
  - 附带:观察窗当前是 +7~**+45**(蓝图要 +21,按需调);扫描 job 已注册但 `enabled=FALSE`,接通后再开。
- **点8 失败池 precheck** `[L]` —— 直接省抓取配额(真金白银)。
  - 现状:download 占失败 **54%(191/356)** 却零 precheck,`video_download.py:24` 直奔 urlopen。
  - 一刀:下载前 `yt-dlp --simulate` / 直链 HEAD 可用性探测,砍掉无效下载;TikTok/IG 拆单独 lane;失败池重处理加按批成本 ledger。

### 🥈 Tier 2 — 收尾(都是「写了没接出来」,`M`,性价比高)
- **点3 分享审计** —— `share_audit.list_for_target()` 已写但**零调用**;接 endpoint + ShareModal 历史 tab(撤销可追溯)+ 补 KOL share + 硬删改软删。
- **点9 Scheduler 真闸** —— `max_daily_runs/max_daily_cost_cents/allowed_hours` 是占位列**没生效**;接成运行时真闸 + 回写 `last_run_at/last_error`。
- **点10 Action Inbox 验收** —— 执行后只记 ledger **不验收**;加 `expected_gain` 字段 + 真「回查目标是否达成」(深析 result 是否 ready / 观察窗是否真开)。
- **点11 Events 收敛** —— projects 版与 events Mockup 版**双实现**并存;并入主流退役 Mockup;清 Dashboard 侧上下级残留(HierarchyDropdown)。
- **点6 任务看板收尾** —— 板 + 6态 + 三态已成熟;补 elapsed 显示 + 完成 toast(`GlassToast` 已存在未接)。「真正统一写路径」是更大的架构活,归 Tier 4。

### 🥉 Tier 3 — 收口小刀(`S`,顺手清)
- **点1** /health 前端把 `trust`(sha_aligned/worker_online/db_migration)显出来 + 一条 /health smoke + `scheduler_enabled_count` 字段名对齐。
- **点2** 抽 `kol_scope_filter` 命名 helper + `policy.require_*` 全路由铺开。
- **点5** session 的 `errors` 从 `result_summary_json` 提到顶层可查字段。
- **点7** 加 `media_cache_status` 显式三态列(pending/cached/failed),替代现在「有 url 即真」。

### 🧱 Tier 4 — 结构债(用户看不见,等业务跑起来再还)
- **点12 前端债** `[L]` —— 拆 `SmartKolInputPanel.tsx`(1860 行)/ `ProjectDetailModals`(1397)/ `V615ReplicaApp`(1196);建 4 个目标 hook(一个都还没有);676 处 `any` 收口;`@ts-nocheck` 仅剩 5。
- **点13 后端分层** `[XL]` —— `vkpi_kol_pool.py`(1754 行 router 直接写 SQL)瘦身;24 个含 raw SQL 的 router 下沉到 repo 层(全仓仅 2 个 repo 文件)。**最贵、最不紧急**,别在上线前动。

---

## 三、别重复造(已达/超蓝图水平,蓝图让"新建"是误判)
| 蓝图以为要新建 | 真相 |
|---|---|
| /health 运行态 | `main.py:214` trust 块 worker_sha/sha_aligned/db_migration/worker_online 全在(80%)|
| 统一 scope service | `domains/access/scope.py` 已闭环,Dashboard 4 聚合早带 scope(90%)|
| KOL URL 闭环 / search session | `migrations/103` + `url_deep_crawl.py` video/profile/新人全分支(88%)|
| MediaPlayerModal | `KOLDetailDrawer.tsx:326` RepresentativeVideoPlayerModal,且比蓝图更稳(90%)|
| Action Inbox schema | `migrations/141` + 9 类生产者 + 双闸执行器 + 前端面板(82%)|
| Scheduler registry | `migrations/130` + 读写 API + 前端开关 + APScheduler config-gate(75%)|
| Project/Event 分享 | `migrations/131/132` + ShareModal 真后端闭环(55%,缺统一模型/KOL/软删/审计可见)|

---

## 四、一句话节奏
先上线(地基已绿)→ Shopify 接通跑真数据 → 集中火力做**点4 + 点8** → 再按 Tier 2 收尾。
结构债(12/13)等真有人用了再还。蓝图当**对照检查表**用,不当从零施工单。
